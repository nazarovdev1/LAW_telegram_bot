# This file is deprecated. Please use .github/workflows/deploy-bot.yml instead
# The new workflow file has better configuration and error handling

name: Run Telegram Bot (DEPRECATED)

# on:
#   push:
#   workflow_dispatch:
#   schedule:
#     - cron: "*/5 * * * *"  # Har 5 daqiqada ishga tushadi

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: Run bot
        run: node index.mjs
